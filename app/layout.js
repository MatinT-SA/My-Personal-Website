// app/layout.js - The HTML Shell (Modified)

import "@/app/styles/global.css";
// ❌ Remove Header import and rendering
// import Header from "./components/header/Header";

export const metadata = {
  title: "Matin Taherzadeh - متین طاهرزاده",
  description: "Generated by create next app",
};

// 💡 Accept lang and dir props with defaults
export default function RootLayout({ children, lang = "fa", dir = "rtl" }) {
  return (
    // 💡 Use dynamic lang and dir attributes
    <html lang={lang} dir={dir} className="scroll-smooth">
      <body
        className={`antialiased 
          bg-linear-to-r from-white to-[#ffe6ff] 
          overflow-x-hidden text-black font-shabnam`}
      >
        {/* ❌ HEADER IS GONE FROM THE ROOT LAYOUT */}
        {children}
      </body>
    </html>
  );
}
